/**
 * Brain node labels are DOM elements positioned by BrainGraph each frame
 * (cheaper than Three sprites + crisper text). This file holds the styles
 * that target the DOM-only labels — the actual element is created in
 * BrainGraph.tsx.
 *
 * Kept as a separate file so the styles live near the Brain components
 * rather than buried in globals.css.
 */
import "./node-label.css";

export {};
